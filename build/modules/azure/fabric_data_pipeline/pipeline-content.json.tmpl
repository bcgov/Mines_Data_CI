{
  "name": "{{.display_name}}",
  "properties": {
    "activities": {{.activities_json}},
    "parameters": {
      "pipeline_name": {
        "type": "String",
        "defaultValue": "{{.pipeline_name_param}}"
      },
      "environment": {
        "type": "String",
        "defaultValue": "{{.environment}}"
      }
    },
    "variables": {},
    "annotations": []
  }
}
