{
  "name": "{{.display_name}}",
  "properties": {
    "activities": {{.activities_json}},
    "parameters": {
      "pipeline_name": {
        "type": "String",
        "defaultValue": "{{.pipeline_name_param}}"
      },
      "environment": {
        "type": "String",
        "defaultValue": "{{.environment}}"
      },
      "triggered_by": {
        "type": "String",
        "defaultValue": "{{.triggered_by}}"
      },
      "override_from_date": {
        "type": "String",
        "defaultValue": "{{.override_from_date}}"
      },
      "override_to_date": {
        "type": "String",
        "defaultValue": "{{.override_to_date}}"
      },
      "table_filter_csv": {
        "type": "String",
        "defaultValue": "{{.table_filter_csv}}"
      },
      "advance_watermark_on_override": {
        "type": "String",
        "defaultValue": "{{.advance_watermark_on_override}}"
      }
    },
    "variables": {},
    "annotations": []
  }
}